#include <stdio.h>
#include <string.h>

int power(int a, int b);
int character_to_decimal(char character);
char decimal_to_character(int decimal);
int convert_to_dec(char* number,int base_from);
void convert_to_new(int number, int base_to);